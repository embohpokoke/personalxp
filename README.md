# personalxp
personal Expense tracker using ai agents (openclaw/hemes) as Expense Capture
Personal finance tracker untuk personal users. Mobile-first PWA installable di HP. Input utama via receipt image yang dikirim ke openclaw/Hermes agent → agent OCR-extract → POST ke API backend. Data tersimpan di server/local hosting dengan MySQL.
